{
  "name": "build",
  "version": "1.0.0",
  "description": "personal realisation build project system.",
  "main": "gulpfile.js",
  "dependencies": {
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-git": "^1.2.4",
    "gulp-uglify": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {},
  "devDependencies": {
  }
}
